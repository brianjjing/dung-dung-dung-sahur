# Triple D - Uno Q (Linux) Python dependencies
#
# Minimal core (the pipeline runs in MOCK mode with just this):
pyserial>=3.5

# Vision (DECIDE stage) - on-device, offline YOLOv8 drone detector:
opencv-python>=4.8
numpy>=1.24
onnxruntime>=1.16

# DETECT off the Uno Q's OWN mic with the trained CNN (USE_CNN_DETECT=True):
# sounddevice>=0.4     # mic capture (needs PortAudio: apt install libportaudio2)
# librosa>=0.10        # Mel-spectrogram
# torch>=2.0           # runs drone_acoustic_cnn.pth (CPU build is fine)
#   -> see acoustic_cnn.py; model file in models/drone_acoustic_cnn.pth
